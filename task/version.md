## this is the file that the conclude the version
# the version will be stored as v a.b.c
a represents the total version, a bigger version of the total update.
b represents the version for every small update for certain tasks.
c represents the minor fix for tasks.
for example.
for a prompt. i listed new task. i wanted to add 3 features in a task. this three features will be stored in the version b. but it is hard to succeed the fix in on shot, so we do the small minor fix in version control in c. And for a, we called it a fix for a.
real example
the first task.md (which i rename to v 1.1.1), since i only added three features, which will alternate in the version number b. And since i do not think that the results really like the results so i think that i need to fix some of the smaller version in version c.

# how i store the data
i store the data in /home/task/version

## then i want you to write the lab diary md according to version
